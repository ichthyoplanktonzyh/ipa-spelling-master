# 多语言语音训练平台 PRD

## 1. 文档信息

- **文档用途**：定义 PhoneticMaster 产品愿景、范围、核心概念与迭代方向
- **当前阶段**：v1.3 — M4 本地个性化已完成，M5 可信内容与练习体验扩展已规划
- **参考文档**：`ROADMAP.md`、`REQUIREMENTS.md`、`STATE.md`、`codebase/ARCHITECTURE.md`
- **更新记录**：
  - 2026-06-22：初始版本，定义从英语单语 IPA 工具扩展为多语言语音训练平台
  - 2026-06-22：产品阶段重排，明确先做独立训练器，再做专项训练与本地个性化推荐
  - 2026-07-02：M2 纯前端独立训练器完成，Phase 3.1 最小对立体听辨完成
  - 2026-07-02：Phase 3.2 中文结构化拼音输入完成，保留自由输入兜底
  - 2026-07-03：Phase 3.3 音素详情面板完成，打通推荐、错题、主题和最小对立体入口
  - 2026-07-15：参考 iSpeakerReact 调整 M5，先建设可信内容、标准音频、引导式音素学习、本地录音和数据驱动练习，再进行新语言试点

## 2. 产品愿景

> 我为什么听不出 /ɪ/ 和 /i/ 的区别？
> 为什么老说我二声三声不分？

PhoneticMaster 是一个 **轻量、纯前端的多语言语音感知训练器**。它帮助语言学习者通过系统性练习建立目标语言的声音范畴，先听得准，再说得稳。

核心主张：**训练器本身必须完整；个性化推荐让训练更准。**

产品能力分为三层：

1. **独立训练层**：用户不需要诊断、不需要账号、不需要理解语言学理论，打开网页即可完成一次有效训练。
2. **引导学习层**：围绕目标声音提供可信示范、上下文例词、多样感知练习和可选的本地录音复习。
3. **智能教练层**：在训练闭环成立后，结合 L1→L2 难点和本地训练历史，推荐用户下一步最该练什么。

## 3. 背景与现状

### 起源

项目最初名为 `ipa-spelling`，是一个纯英语美式 IPA 拼写训练工具。用户听英语单词发音，用屏幕 IPA 键盘输入音标，系统判断对错。功能完整但仅服务于"学英语"这一个场景。

### 扩展动机

1. **L2 扩展**：汉语学习者需要拼音训练，且拼音体系（声母/韵母/声调）与 IPA 键盘布局完全不同
2. **L1 感知差异**：不同母语者对同一目标语言的感知盲区不同（PAM / SLM 理论），应该提供个性化推荐
3. **架构可持续**：添加日语、西语等新语言时不应修改框架代码，只需新增 Profile + 词库

### 当前能力

| 维度 | 状态 |
|------|------|
| 英语 IPA 拼写训练 | ✅ 完整 |
| 汉语拼音拼写训练 | ✅ 基础可用 |
| 听力训练模式 | ✅ 双语言支持 |
| L1→L2 难度映射 | ✅ zh→en + en→zh |
| 智能推荐面板 | ✅ 基础版 |
| 音素/拼音单元详情 | ✅ 可查看 L1 难点、例词和最小对立体入口 |
| 语音合成 (TTS) | ✅ Web Speech API |
| 新语言扩展 | ✅ Profile 架构就绪 |

## 4. 产品定位

| 定位维度 | 描述 |
|----------|------|
| **目标用户** | 正在学习第二语言 (L2) 的成年学习者 |
| **核心场景** | 语音感知训练：通过听辨、拼写、对比练习建立 L2 声音范畴 |
| **与词典的区别** | 词典告诉你"这个词怎么读"，PhoneticMaster 告诉你"你为什么读不准" |
| **与口语 App 的区别** | 口语 App 练产出，PhoneticMaster 练感知——先听得准，才能说得对 |
| **平台** | 纯前端 Web SPA（桌面优先），MVP 可静态部署到公网 |

### 4.1 产品分层

| 层级 | 目标 | 是否依赖 L1 诊断 | 说明 |
|------|------|------------------|------|
| 独立训练器 | 完成一次完整训练会话 | 否 | 选择目标语言、训练类型、难度/主题后直接开始 |
| 专项训练器 | 针对音素/声调/最小对立体强化 | 否 | 由用户手动选择主题，也可被推荐系统唤起 |
| 引导学习 | 可信示范、上下文例词、多样练习、本地录音 | 否 | M5 增强层；缺少内容或浏览器能力时可降级 |
| 智能教练 | 推荐下一步训练内容 | 是，可选 | 结合 L1-aware 难点和本地掌握度 |
| 云端增强 | 同步、大规模媒体托管、ASR、社区内容 | 否，远期可选 | 不属于 MVP 前置条件 |

## 5. 核心理论框架

### 5.1 PAM — 感知同化模型 (Perceptual Assimilation Model)

Best (1994) 提出的 PAM 认为：L2 语音会被听者同化到最接近的 L1 类别中。同化类型决定了感知难度：

| 同化类型 | 感知结果 | 难度 |
|----------|----------|------|
| Two Category (TC) | 两个 L2 音分别同化到两个 L1 音 | 容易区分 |
| Single Category (SC) | 两个 L2 音同化到同一个 L1 音 | 很难区分 |
| Category Goodness (CG) | 都同化到同一 L1 音，但一个更像 | 中等难度 |
| Uncategorized-Categorized (UC) | 一个可同化，另一个不可 | 较难 |
| Uncategorized-Uncategorized (UU) | 都不可同化 | 极难 |

### 5.2 SLM — 语音学习模型 (Speech Learning Model)

Flege (1995) 的 SLM 补充了产出侧：L2 语音能否被习得取决于学习者是否建立了新的语音范畴。如果 L2 音与 L1 音"太像"，学习者会将其合并到已有范畴，永远无法准确产出。

### 5.3 对产品的意义

- 不同 (L1, L2) 对的难点完全不同 → 需要 L1-aware 的难度映射
- "接近但不相同"的音最难 → 需要最小对立体 (minimal pairs) 训练
- 感知先于产出 → 先练听辨，再练拼写
- L1-aware 推荐是训练加速器，不是进入训练的门槛

## 6. 核心用户流程

```
直接训练路径：

打开应用 → 选择目标语言 L2
         → 选择训练类型 / 难度 / 主题
         → 开始训练
            ├── 听音拼写：听音 → 输入音标/拼音 → 系统判定
            ├── 看词听音：看词/记号 → 反复听音 → 翻页浏览
            └── 对比听辨：播放目标 → 在最小对立体中选择
         → 会话结果 → 再练一次 / 换主题

智能路径：

选择母语 L1 + 目标语言 L2
         → 查看 L1-aware 推荐
         → 一键进入专项训练
         → 完成后更新本地掌握度
         → 推荐下一项
```

## 7. MVP 范围 (v1.0)

### 产品阶段策略

MVP 优先完成 **纯前端独立训练器**。L1-aware 推荐、诊断和掌握度属于增强层，不阻塞用户开始训练。

### 包含

- 英语 IPA 拼写训练（基础/进阶/挑战三个难度档）
- 汉语拼音拼写训练（HSK 1-3 词库）
- 独立训练入口：无诊断也能选择 L2、训练模式、难度、主题并完成会话
- 看词听音训练模式
- nearMatch 与音素差异反馈
- 会话结果页 / 本地最近训练记录
- Web Speech API TTS
- 音素筛选 + 词汇量控制
- 进度条 + 得分
- 10 种 L1 选项 + zh→en / en→zh 难度映射（作为可选智能层）

### 不包含

- 用户注册 / 登录
- 语音识别（产出训练）
- 后端服务 / 数据库（MVP 静态部署）
- 移动端适配
- 社交功能（排行榜、分享）
- 离线 PWA

## 8. 数据概念

### 8.1 核心类型

| 概念 | 类型 | 说明 |
|------|------|------|
| TrainingItem | `interface` | 语言无关的训练条目：display + pronunciation + pronunciationAlt + frequencyTier + definition? |
| LanguageProfile | `interface` | 语言声明：音素清单 + 键盘布局 + 解析器 + 判定器 + 词库 |
| JudgeResult | `interface` | 判定结果：correct + nearMatch + diffs[] |
| L1L2Difficulty | `interface` | L1→L2 难度映射：hardPhonemes + hardFeatures |
| PhonemeDifficulty | `interface` | 单音素难度：phoneme + level + reason + l1Equivalence + minimalPairs |
| KeypadSection | `interface` | 键盘区域：category + phonemes[] |
| Difficulty | `type` | `'basic' \| 'intermediate' \| 'advanced'` |
| TrainingSession | `interface` | 一轮训练会话：mode + items + answers + score |
| MasteryRecord | `interface` | 本地掌握度：按 L2/音素记录正确率与练习次数 |

### 8.2 旧类型兼容

| 旧类型 | 新类型 | 映射 |
|--------|--------|------|
| WordData | TrainingItem | word→display, ipa_us→pronunciation, ipa_uk→pronunciationAlt |

## 9. 成功标准

| 指标 | 目标 |
|------|------|
| 支持的目标语言数 | ≥ 2（英语 + 汉语） |
| 独立训练闭环 | 无需 L1 诊断即可完成训练会话 |
| 静态公网发布 | 无后端依赖，可部署到 Vercel/Netlify/Cloudflare Pages/GitHub Pages |
| L1×L2 难度映射 | ≥ 2 对（zh→en + en→zh），作为可选推荐层 |
| 新语言接入成本 | 仅需添加 1 个 profile 文件 + 1 个词库文件 + 0 行框架代码改动 |
| 编译零错误 | `npx tsc --noEmit` 通过 |
| 构建成功 | `npm run build` 通过 |

## 10. 风险与待决策事项

| # | 风险/决策 | 影响 | 当前状态 |
|---|-----------|------|----------|
| 1 | Web Speech API 语音质量参差不齐 | TTS 体验不可控 | 已实现优先级打分 + 手动选择 |
| 2 | 拼音输入方式：声调用数字还是符号？ | 影响拼写模式 UX | 已选：数字形式 (ni3 hao3)，同时显示声调符号 |
| 3 | 拼音 judge 对声调的容错度 | 声调错误算 nearMatch 还是 incorrect？ | 当前：声调错误算 incorrect |
| 4 | MVP 是否需要后端？ | 影响发布速度与复杂度 | 已决策：MVP 纯前端，本地存储 |
| 5 | 未来是否引入 ASR 做产出训练？ | 需要集成 Web Speech Recognition 或 Whisper | 待决策，v2.0+ 考虑 |
| 6 | L1×L2 映射数据由谁维护？ | 语言学专业性强 | 当前硬编码，未来考虑社区贡献 |
| 7 | Web Speech API 是否足够支撑最小对立体？ | 影响高精度听辨可信度 | MVP 使用；核心 pair 未来可托管标准音频 |

## 11. 里程碑状态

| 里程碑 | 内容 | 状态 |
|--------|------|------|
| M0 | 原始 ipa-spelling 英语工具 | ✅ 已完成（项目起点） |
| M1 | 多语言架构 + 汉语 + L1 推荐 | ✅ 已完成 |
| M2 | 纯前端独立训练器 MVP | ✅ 已完成 |
| M3 | 专项训练 + 最小对立体 | ✅ 已完成 |
| M4 | 本地个性化 + L1-aware 教练 | ✅ 已完成 |
| M5 | 可信内容、练习体验与新语言试点 | 🧭 已规划 |
| M6 | 云端增强（账号/同步/音频/ASR） | 🧭 可选远期 |

> 详细阶段规划见 `ROADMAP.md`，当前状态见 `STATE.md`
