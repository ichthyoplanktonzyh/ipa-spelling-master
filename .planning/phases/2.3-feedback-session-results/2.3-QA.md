# Phase 2.3 手动 QA 清单

## 验证日期

2026-07-02

## 自动门禁

```bash
npm run lint
npm run build
npm run validate:data
```

结果：全部通过。`npm run build` 仍显示既有 Vite chunk-size warning 和 Node deprecation warning，不阻塞本 phase。

## 浏览器冒烟

本地验证地址：`http://localhost:3001/`

- [x] 英语拼写模式可进入训练器，header 显示 live score 与已答题数。
- [x] Words 调整为 1 后会刷新为 1 题会话。
- [x] 提交错误 IPA 输入后显示 `Correct:`、`Difference Map` 和 expected/yours 差异位置。
- [x] 最后一题点击 `Complete Session` 后进入 `Session Complete` 结果页，不出现 alert。
- [x] 结果页显示 exact score、missed 统计、Review 和 Recent Sessions。
- [x] 清除最近记录后，历史区显示 `No saved sessions yet.`，当前结果页仍保留。
- [x] 浏览器 console 无 error 日志。

## 待后续自动化

- judge/parser/storage 的 fixture-based 单元测试仍建议在引入 Vitest 后补齐。
- Phase 2.4 发布准备阶段可把本清单扩展为完整手动回归清单。
